apiVersion: v1
kind: PersistentVolume
metadata:
  name: p-balochi-turkey-soup-filesystem
spec:
  storageClassName: ""
  capacity:
    storage: 5Mi
  accessModes:
    - ReadOnlyMany
  hostPath:
    path: /home/docker/arts