image:
  tag: 2.1.0

ingress:
  enabled: true
  hosts:
    - kebab-api.rebirthofhope.ru

replicaCount: 1

app:
  cors:
    origins: https://kebab-api.abyssofdespair.ru
  log:
    level: trace
  database:
    connection: {{ .Values.app.db }}