image:
  tag: 2.1.0

ingress:
  enabled: true
  hosts:
    - som-tam-api.rebirthofhope.ru

replicaCount: 1

app:
  cors:
    origins: https://som-tam-api.abyssofdespair.ru
  log:
    level: trace
  database:
    connection: {{ .Values.app.db }}