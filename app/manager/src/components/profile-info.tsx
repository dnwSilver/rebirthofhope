import Button from "./button";

const ProfileInfo = () => {
  return (
    <section style={{ margin: "1rem" }}>
      <Button>Попытаться побороть инфраструктуру</Button>
    </section>
  );
};

export default ProfileInfo;
